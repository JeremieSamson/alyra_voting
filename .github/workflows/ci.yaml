name: CI

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: make build

      - name: Up containers
        run: make up

      - name: Install modules
        run: make node_modules TTY=-T

      - name: Run ESLint
        run: make lint TTY=-T

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v3
        with:
          node-version: '12'

      - name: Install
        run: npm install

      - name: Launch Ganache
        run: npx ganache-cli -h 127.0.0.1&

      - name: Run truffle tests
        run: npx truffle test --network docker

      - name: Coverage
        run: d

      - name: Coveralls
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: make build

      - name: Up containers
        run: make up

      - name: Install modules
        run: make node_modules TTY=-T

      - name: Install truffle globaly
        run: docker-compose exec -T truffle npm install -g truffle@5.4.32

      - name: Run truffle tests
        run: make truffle-test TTY=-T